/*
 * detab-2.c: detab lines, accepting the shorthand
 * ./a.out -m +n,
 * meaning tabstops every n columns, starting at column m.
 *
 * A tabstop for our purposes since I can't find a decent damn definition, means
 * that a tab stands for a certain number of columns.
 */
